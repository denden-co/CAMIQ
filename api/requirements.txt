# CampaignIQ API — core runtime dependencies (v0)
# Heavy ML libs live in requirements-ml.txt and install only when
# we upgrade to XLM-RoBERTa / transformer ensembles in v1.

# Core
# Security floors (2026-07-02): python-multipart >=0.0.31 (CVE-2026-53540 et al),
# pydantic-settings >=2.14.2 (GHSA-4xgf-cpjx-pc3j), fastapi >=0.139 pulls
# starlette >=1.3.1 (PYSEC-2026-249).
fastapi>=0.139.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.31
python-dotenv>=1.0.0
pydantic>=2.6.0
pydantic-settings>=2.14.2

# Sentiment (v0 — lexicon-based, zero model download)
vaderSentiment>=3.3.2

# Utilities
httpx>=0.27.0
