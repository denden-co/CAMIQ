# CampaignIQ API — core runtime dependencies (v0)
# Heavy ML libs live in requirements-ml.txt and install only when
# we upgrade to XLM-RoBERTa / transformer ensembles in v1.

# Core
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
pydantic>=2.6.0
pydantic-settings>=2.1.0

# Sentiment (v0 — lexicon-based, zero model download)
vaderSentiment>=3.3.2

# Utilities
httpx>=0.27.0
