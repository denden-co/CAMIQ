# CampaignIQ API — heavy ML dependencies (v1+)
# Install on top of requirements.txt when you're ready to run
# XLM-RoBERTa, BERTweet, BERTopic, XGBoost, and the thesis ensemble.
#
# Usage:
#   pip install -r requirements.txt
#   pip install -r requirements-ml.txt

# Transformers stack
transformers>=4.38.0
torch>=2.2.0
huggingface-hub>=0.21.0
# sentencepiece is required by the XLM-RoBERTa tokenizer (SentencePiece BPE).
# transformers does not pull it in automatically — missing it causes the
# sentiment service to silently fall back to VADER.
sentencepiece>=0.2.0

# Classical ML
scikit-learn>=1.4.0
xgboost>=2.0.0

# Topic modelling
# NOTE: gensim removed — no Python 3.14 wheel yet (PyDictObject.ma_version_tag
# was dropped in CPython 3.14, source build fails). BERTopic uses
# sentence-transformers + UMAP + HDBSCAN by default and does not require
# gensim. Re-add under a Python 3.12 venv if/when we want Word2Vec or LDA.
bertopic>=0.16.0

# Alt sentiment / text
textblob>=0.18.0

# Data processing
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.12.0
langdetect>=1.0.9
chardet>=5.2.0

# Database (moved here — only needed once we persist analyses)
supabase>=2.3.0
asyncpg>=0.29.0

# LLM providers (voter personas + AI strategy features)
google-generativeai>=0.4.0
openai>=1.12.0
anthropic>=0.18.0
cohere>=4.47
mistralai>=0.1.0

# Background jobs
celery[redis]>=5.3.0
websockets>=12.0
