import { ComingSoon } from "@/components/coming-soon";

export const metadata = {
  title: "Cookie Policy — CampaignIQ",
  description: "How CampaignIQ uses cookies and similar technologies.",
};

export default function CookiesPage() {
  return (
    <ComingSoon
      title="Cookie Policy"
      description="Details of the cookies and similar technologies CampaignIQ uses, categorised by purpose (strictly necessary, analytics, preferences), with opt-in controls."
    />
  );
}
